# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: step 1 - Checkout code
        uses: actions/checkout@v3

      - name: step 2 - Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: step 3 - Build and assemble modules
        working-directory: .
        run: |
          echo "Loading... Building and assembling modules"
          mvn clean install -P assemble-modules -pl paramsAPI
        
      - name: step 4 - Upload code coverage reports
        uses: actions/upload-artifact@v3
        with:
          name: jacoco-report
          path: paramsAPI/target/site/jacoco/index.html
        